/*

function calcAvgSquare(num1,num2,num3) {
  let avg =  (num1 + num2 + num3) / 3
  let numSq =  num1 ** 2
  let answer = Math.round(avg * numSq)
  return answer
}

let doAgain = true
while (doAgain == true) {
  
  let a = Number(prompt("enter the first number"))
  let b = Number(prompt("enter the first number"))
  let c = Number(prompt("enter the first number"))
  

  let average = calcAvgSquare(a,b,c)
  
  alert(`the answer using ${a}, ${b}, ${c} is ${average}`)

}

doAgain = confirm("do you want to run again?")

*/
